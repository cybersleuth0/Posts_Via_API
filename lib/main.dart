import 'package:flutter/material.dart';
import 'package:post_via_api/postscreen.dart';

void main() {
  runApp(
      MaterialApp(
          home: PostScreen(),
        debugShowCheckedModeBanner: false,
  ));
}
